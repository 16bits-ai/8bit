# Server-side only — set this in Vercel project env vars, NOT prefixed with VITE_
# (anything VITE_* is bundled into the client and publicly readable)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Client-side Mapbox token used by the travel globe.
VITE_MAPBOX_TOKEN=your_mapbox_token_here
